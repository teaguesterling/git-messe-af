{
    "name": "messe-af/exchange-server",
    "description": "MESS Exchange Server - PHP implementation with git storage",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "symfony/yaml": "^6.0 || ^7.0"
    },
    "autoload": {
        "psr-4": {
            "MesseAf\\": "src/"
        }
    },
    "scripts": {
        "start": "php -S localhost:8080 -t public"
    }
}
