apiVersion: v1
kind: Service
metadata:
  name: {{ include "mess-exchange.fullname" . }}
  labels:
    {{- include "mess-exchange.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mess-exchange.selectorLabels" . | nindent 4 }}
